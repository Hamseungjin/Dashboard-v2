import Loading from '/images/loader.svg';

const Loader = () => {
  return (
    <div>
      <img 
        src={Loading}
        alt="loader"
        width={44}
        height={44}
      />
    </div>
  )
}

export default Loader